extends Node

signal load_level(id)

# Lights
signal hurt()
signal flash()
signal light()
signal cheat_light()

signal landing_mark(pos)

signal hud_ink(value)
signal hud_matches(value)
signal hud_time(value)
signal hud_lifes(value)

signal game_restart()
